<div id="hd">
	<h1 id="logo">test</h1>
	<div class="nav">
    <% if (navs.length) { %>
      <ul class="nav-list">
        <% navs.forEach(function(nav){ %>
          <li class="nav-item" href="<%= nav.href %>"><%= nav.name %></li>
        <% }) %>
      </ul>
    <% } %>
    </div>
</div>